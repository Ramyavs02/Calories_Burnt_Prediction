# Calories_Burnt_Prediction
This project predicts calories burned based on exercise data using machine learning. It features a user-friendly **Streamlit UI** that allows you to upload datasets, train a model, and view predictions and visualizations interactively.
## 🚀 How to Run the App

### 1. Install Dependencies

Make sure Python is installed, then run:

```bash
pip install -r requirements.txt

Launch the Streamlit App
If streamlit is not recognized as a command, run:
python -m streamlit run calories_app.py

